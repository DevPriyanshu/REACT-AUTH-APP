export type CustomDate = Date | string | number;
export type ToggleType = "ADD" | "REMOVE";
export type StatusType = "PENDING" | "IN_PROGRESS" | "READY";
export type ColorSchema = 'primary' | 'secondary' | 'info' | 'success' | 'warning' | 'error';
